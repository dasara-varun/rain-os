# Maintainer: Rain OS Contributors <https://github.com/dasara-varun/rain-os>
pkgname=rain-probe
pkgver=1.2.0
pkgrel=1
pkgdesc="High-performance native C hardware, display, and thermal probe"
arch=('x86_64')
url="https://github.com/dasara-varun/rain-os"
license=('GPL3')
depends=('glibc')
makedepends=('gcc' 'make')
source=(
  "rain-probe.c"
  "Makefile"
)
sha256sums=(
  'SKIP'
  'SKIP'
)

build() {
  cd "$srcdir"
  make
}

package() {
  cd "$srcdir"
  make install DESTDIR="$pkgdir" PREFIX=/usr
}
